Necessary:
 - None! :>

Optional:
 - Find better weights or weight functions for collectors
 - Implement auto-binning frequency distribution for numbers

Stylistic:
 - Try to avoid calling utilities.functional.apply_memberfn() with a string as
   its first argument by virtualising the underlying function.
